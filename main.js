"use strict";

let screen = PIXI.screen;

let app = new PIXI.Application();

document.body.appendChild(app.view);

class State {
	constructor(num_particles) {
		this.particles = [];
	}
}

class PXObj {

}

class Blob extends PXObj {
	constructor(x, y, color) {
		this.x = x;
		this.y = y;
		this.
	}
}
